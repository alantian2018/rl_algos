## marl brainstorming
1. naively just encode all other options as static/part of the env. I think this could work => train one shared network.

2. if we sync actions per observation I wanna try out CTDE cuz it looks interesting